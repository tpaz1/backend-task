services:
  api:
    build: .
    ports:
      - "8000:8000"
    environment:
      - LOG_JSON=false
      - LOG_LEVEL=INFO
    volumes:
      - ./src:/app/src:ro
    command: uvicorn src.main:app --host 0.0.0.0 --port 8000 --reload
